//objeto sem previbilidade
let product : object = {}

type ProdutoLoja =  {
    nome : string,
    preco : number,
    unidades : number
}

let produto : ProdutoLoja = {
    nome : "notebook lenovo",
    preco : 2600.00,
    unidades : 50,
}